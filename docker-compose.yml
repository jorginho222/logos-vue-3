services:
  web:
    build:
      context: .
      args:
        USER_ID: 1000
        GROUP_ID: 1000
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
